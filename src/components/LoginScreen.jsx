import React, { useState } from "react";
// import apiSource

function LoginScreen(
  {
    // Props
  }
) {
  // State declarations
  // Functions
  // Render
  return (
    <div>
      <h1>Log In</h1>
    </div>
  );
}

export default LoginScreen;
